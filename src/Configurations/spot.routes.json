{
  "ReverseProxy": {
    "Routes": {
      "spot-routes": {
        "ClusterId": "spot-cluster",
        "Match": {
          "Path": "spot/{**catch-all}"
        },
        "Transforms": [ { "PathPattern": "{**catch-all}" } ]
      }
    }
  }
}